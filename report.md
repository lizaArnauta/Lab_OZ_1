# Звіт. Лабораторна робота #1
<b>Берегун Роман<br>Генч Герай<br>Арнаута Єлизавета</b><br>

## Частина 1

### Огляд даних, основні особливості й проблеми
Дані - датасет OpenTTS UK датасет<br>
Озвучується 5-ма акторами<br>
Кожен запис містить:
- аудіофайл у форматі .ogg 
- текст аудіо
- текст із наголосами
- тривалість

Серед усіх акторів найбільш чисто звучить Микита, тому його записи використано для даної роботи <br>
Особливості:
- 6436 записів
- тривалість від 1.9 до 11.5с, великий розрив
- середня тривалість 5.07с (std=1.51)
- sample rate 48000
- 1 канал (моно)

Серед переглянутих семплів спостерігається, що аудіо є літературним читанням, повільне, з виразною інтонацією та чітко-вираженими паузами

Проблемним місцем є велика різниця у тривалостях, які місцями є занадто екстримальними від середнього 5с. 

### Опис використаних підходів
Препроцесинг:
- частоту опустили до 16кГц, для детекції фонем цього достатньо
- текст очистили від знаків пунктуації, вони не змінюють звуки тому є шумом
- текст звели до нижнього регістру, від цього самі звуки не змінюються, а правила написання визначаються правилами мови, а не за звуком

Алфавіт закодований у словник символів, де кожній букві призначений індекс (модель працює з числами), а також додано спеціальні токени, "blank"=0 для позначення пауз між звуками, "unk"=1 щоб хендлити невідомі звуки. В тексті пробіли замінені на вертикальну лінію "|", яка краще кодується. Це є частиною СТС.

Щоб вирішити проблему різної тривалості аудіо - усі записи зведено до однієї довжини, яка рівна найдовшому файлу в датасеті. Порожні значення заповнюються -100, та ігноруються в обрахунку метрик

Використано Wav2Vec2 модель. У порівнянні з попередньою, використовує трансформерний енкодер, додано квантизацію (переведення до дискретної форми представлень аудіо), що ефективніше для визначення фонем і легше в обчисленнях.  
Також для проведення порівнянь взято класичну Wav2Vec та HuBert. Остання відрізняється від Wav2Vec в тому, що навчається розбивати дані на кластери, а потім передбачати мітки як у задачі класифікації, йому не потрібно вчитись передбачати на негативних семплах.


### Таблиці метрик

Таблиця результатів моделей на Common Voice 10 та аналогічний графік
| | HuBERT | Wav2Vec2-XLSR | Wav2Vec2-Base |
|---|---|---|---|
| WER, % | 75.56 | 80.21 | 100.00 |
| CER, % | 22.30 | 26.81 | 100.00 |

![](/plots/1.1.png)


Порівняння метрики CER на оригінальному (студійному) датасеті та на Common Voice
| | HuBERT-Large | Wav2Vec2-XLSR | Wav2Vec2-Base |
|---|---|---|---|
| CER (OpenTTS), % | 5.73 | 3.20 | 100.00 |
| CER (Common Voice), % | 22.30 | 26.81 | 100.00 |

![](/plots/1.2.png)


### Гіпотези й думки щодо отриманих результатів

Так як моделі визначає звуки, а не слова, то їх результат виглядає непоганим на CER. Попри низький WER, людське око все одно може приблизно вловлювати зміст із виявленого тексту. Вірогідно, моделі можна вдосконалити якщо на post-model processing додати ще одну модель, яка зможе на основі цих слів шукати контекст, виправляти помилки у словах і повертати більш якісні речення.

Модель Wav2Vec2-Base взагалі не змогла навчитися на датасеті, тому її результати 100% error rate. Дана модель переднавчена на 93млн прикладів, всі з яких є англійською. Гіпотеза, що саме через мову вона не змогла навчитися українській, потрібно навчати її на значно більшій кількості даних

На "рідному" датасеті, який є студійної якості, моделі показують кращі результати, з додаванням шумів результати CER падають +- на 20%. Це показує, що моделі загалом справляються із задачею, проте їх метрики падають із появою шумів, і зміною спікера

HuBert показує найкращі результати, вірогідно через свою більш розвинуту архітектуру. Попри те, що він переднавчений теж на англійських семплах, але їх >300млн, тому він від цього дійсно достатньо навчений, і ефективно навчається звукам з інших мов


## Частина 2

### Опис запропонованих алгоритмів фільтрування

Другою таскою ми навмисно використовуємо класичні (не-DNN) DSP-фільтри, щоб покращити якість аудіоданих перед навчанням ASR-моделі. Реальні датасети часто містять зсув по нулю, різний рівень гучності, низькочастотний “гул”, тишу на початку/в кінці, а інколи й кліпінг. Це збільшує варіативність входу, погіршує стабільність оптимізації та може піднімати WER/CER через “зайвий” шум і нерелевантні фрагменти.

Використані методи фільтрування:

- DC removal (видалення DC offset): віднімаємо середнє значення сигналу, щоб центр коливань був біля нуля. Це прибирає штучний зсув, який може з’являтися через тракт запису/кодування.
- Peak normalization (нормалізація по піку): масштабуємо сигнал до заданого пікового рівня (з обмеженням), щоб вирівняти гучність між прикладами й зменшити різницю динаміки.
- High-pass filtering (високочастотний фільтр): пригнічуємо низькі частоти (rumble/гул/вітрові шуми), які зазвичай не несуть мовної інформації, але “забруднюють” ознаки.
- Energy-based trimming (обрізання тиші за енергією): прибираємо довгі паузи на початку та в кінці, залишаючи мовну частину. Це зменшує частку “порожнього” контенту й робить вирівнювання для CTC простішим.
Додатково, як об’єктивний контроль якості, ми рахуємо метрики на підвибірці (наприклад: RMS у dB, частка кліпінгу, ZCR, spectral flatness, відношення енергії високих частот тощо). Вони потрібні щоб підтвердити, що фільтрація дійсно змінює сигнал у очікуваному напрямку (менше кліпінгу/тиші/низькочастотного шуму, стабільніший рівень).

### Абляційне дослідження по впливу алгоритмів фільтрування на фінальну якість моделей

Абляція потрібна, щоб відповісти на питання: які саме складові фільтрування дають приріст, а які — нейтральні або навіть можуть шкодити. Логіка дослідження така: ми фіксуємо все, що не є фільтром (архітектура, токенізація, гіперпараметри навчання, кількість кроків/епох, тестовий набір), і змінюємо тільки “пакет” DSP-процесингу, після чого порівнюємо WER/CER.

У нашій постановці базова модель — це модель Wav2Vec2-XLSR з першого завдання. Експериментальна модель без додаткового перенавчання, а ініціалізована спочатку, і навчена на фільтрованих даних, щоб не отримати хибні результати. Далі обидві моделі оцінюються на однаковому тестовому наборі, тому різниця в WER/CER інтерпретується саме як ефект від фільтрування даних під час навчання.

Зміна фізичних властивостей звуку до та після застосування фільтрів

![](/plots/2.1.png)

Обрізка (trimming) зменшила тривалість аудіо на 29%. Може бути корисною, щоб зменшити розмір датасету, зокрема щоб скоротити найдовші записи, тоді решті потрібно буде додавати менше padding, що може краще показатись на оптимізації

![](/plots/2.2.png)

Чистіше аудіо має позитивно вплинути на тренування моделі, за нашими  здогадками

Таблиця результатів:

| | Model (Raw) | Model (Filtered) |
|---|---|---|
| WER | 0.1957 | 0.1928 |
| CER | 0.0286 | 0.0284 |

![](/plots/2.3.png)

Фільтрації дали мінорне покращення результатів. Змінивши набір чи параметри фільтрів, вірогідно, можна отримати ще трохи вищі результати, проте потрібно зважати на обчислювальну складність, яку можна цим додатково створити